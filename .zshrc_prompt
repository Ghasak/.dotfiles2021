# Load version control information
autoload -Uz vcs_info
precmd() { vcs_info }

# Format the vcs_info_msg_0_ variable
zstyle ':vcs_info:git:*' formats "%B%F{#9370DB} on  branch:%B%F{#E066FF}[ %b] "

# Set up the prompt (with git branch name)
setopt PROMPT_SUBST
#PROMPT='%n in ${PWD/#$HOME/~} ${} > '
#PROMPT='%n in ${PWD/#$HOME/~} ${} > '
######
# Using A Splash of Colors
######
#use extended color palette if available
if [[ $terminfo[colors] -ge 256 ]]; then
    turquoise="%F{81}"
    orange="%F{166}"
    purple="%F{135}"
    hotpink="%F{161}"
    limegreen="%F{118}"
else
    turquoise="%F{cyan}"
    orange="%F{yellow}"
    purple="%F{magenta}"
    hotpink="%F{red}"
    limegreen="%F{green}"
fi
######
ZSH_THEME_GIT_PROMPT_PREFIX="$FG[075]($FG[078]"
ZSH_THEME_GIT_PROMPT_CLEAN=""
ZSH_THEME_GIT_PROMPT_DIRTY="$my_orange*%{$reset_color%}"
ZSH_THEME_GIT_PROMPT_SUFFIX="$FG[075])%{$reset_color%}"
ZSH_THEME_GIT_PROMPT_ADDED="%{$fg[cyan]%} ✈"
ZSH_THEME_GIT_PROMPT_MODIFIED="%{$fg[yellow]%} ✭"
ZSH_THEME_GIT_PROMPT_DELETED="%{$fg[red]%} ✗"
ZSH_THEME_GIT_PROMPT_RENAMED="%{$fg[blue]%} ➦"
ZSH_THEME_GIT_PROMPT_UNMERGED="%{$fg[magenta]%} ✂"
ZSH_THEME_GIT_PROMPT_UNTRACKED="%{$fg[grey]%} ✱"
# Adding color to my zsh
autoload -U colors && colors
# % echo $TERM
# xterm-256color
#PROMPT='%B%F{20}%1~%f%b %# '
# https://www.nerdfonts.com/cheat-sheet
OS_ICON=
CLOCK_ICON=
Date_ICON=
git_ICON=
Netreno_ICON=
GEAR_ICON=
DIR_ICON=
PS1=$'\n'
#PS1+="%{$fg[red]%}%n%{$reset_color%} at %{$fg[blue]%}%m %F{226}%~%f  %{$reset_color%}% "
PS1+="╭─${OS_ICON} %F{208}%n%f  %F{226}%m%  on %F{30}%~%f %F{208} %{$reset_color%} "
RPROMPT='%B%F{#F4A460}${vcs_info_msg_0_} $(git_prompt_behind)%B%F{#F4A460}${GEAR_ICON} %{$reset_color%} %{$fg[red]%}[${Date_ICON}[ %D{%b %d, %Y - ${CLOCK_ICON} %T}]%{$reset_color%}'
PS1+=$'\n╰─%F{blue}>%F{226}>%F{red}>$reset_color ';
#
#壟
#─────
# For background color: $bg[red]
